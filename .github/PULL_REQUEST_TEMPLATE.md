<!--
Thanks for the PR! Fill out what's relevant; delete sections that
don't apply. The maintainer reviews aiming for same-day or next-day
turnaround.
-->

## What this changes

<!-- One or two sentences. The "what", git already shows the lines. -->

## Why

<!--
The motivation. If it closes an issue, link it: "Closes #123".
If it's a follow-up to a past discussion or DEVLOG entry, link that
too.
-->

## How it was tested

- [ ] `npm run ci` passes locally (lint + typecheck + test + build).
- [ ] Manual test of the affected path (describe below).

<!--
Manual test description:
e.g. "Loaded the unpacked Chrome companion, paired with VS Code,
exported a /design/p project, verified the assets folder appeared."
-->

## Docs / changelog

- [ ] User-visible change → added an entry under `Unreleased` in
      [`CHANGELOG.md`](../CHANGELOG.md).
- [ ] Non-obvious decision → noted in [`DEVLOG.md`](../DEVLOG.md).
- [ ] N/A, internal refactor / dev tooling only.

## Breaking changes

<!--
If this changes user-facing behavior, file paths, settings, or APIs
in a way that an existing user could notice, describe the migration
here. If it's a hard break, mark it explicitly so the changelog
entry can flag it for the release.
-->

- [ ] No breaking changes.
- [ ] Breaking changes, described below.

## Anything else for the reviewer

<!-- Screenshots for UI changes, perf notes, known follow-ups, etc. -->
